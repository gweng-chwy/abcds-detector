Put experiments here. Create a timestamped folder for each experiment, for
example `20200101_first_data_exploration` or
`20201212_model_visualization`, because it makes experiments easier to find
later.

Use literate and interactive tools such as Jupyter notebooks. Keep experiments
small and focused. If an experiment grows into reusable logic, move that logic
into the library and workflow so it can be tested, automated, and modularized.
